/*
const getEvent = async () => {
    const response = await fetch('Events')
    const {eventName, eventDate} = await response.json()
    console.log(data)

    document.querySelector('.eventName p').textContent = eventName
    document.querySelector('.eventName .eventDate').textContent = eventDate
}

getEvent()

*/
